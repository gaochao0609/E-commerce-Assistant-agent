/*
 * File: next.config.mjs
 * Purpose: Next.js configuration for the AI dashboard app.
 * Flow: sets strict mode and keeps the config minimal.
 * Created: 2026-01-05
 */
const nextConfig = {
  reactStrictMode: true
};

export default nextConfig;
